def so(n):
    n = abs(n)
    max_k = 0
    while n > 0:
        h = n % 10
        if h > max_k:
            max_k = h
        n = n // 10
    return max_k
n = int(input("Nhap so nguyen n: "))
print(f"Chu so lon nhat trong n la:{so(n)}")
